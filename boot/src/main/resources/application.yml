spring:
  application:
    name: multi-ai-provider

ai:
  mock:
    # Default (prod): the completions-mock-api deployment on Vercel.
    # Override with MOCK_AI_BASE_URL, or activate the `local` profile
    # (SPRING_PROFILES_ACTIVE=local) to hit a localhost instance.
    base-url: ${MOCK_AI_BASE_URL:https://competions-mock-api.vercel.app}
